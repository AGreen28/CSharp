/*NaturalNums(2, 8);

void NaturalNums(int n, int m)
{
    if(n <= m)
    {
        NaturalNums(n, m - 1);
        Console.Write(m + " ");
    }
}
*/

/*
int Multiply(int a, int b)
{
    int result = 1;
    if(b > 0)
    {
        result = a * Multiply(a, b - 1);
    }

    return result;
}

Console.Write("Введите число: ");
int numA = Convert.ToInt32(Console.ReadLine());
Console.Write("Введите степень: ");
int numB = Convert.ToInt32(Console.ReadLine());
if (numB < 0)
{    
    Console.WriteLine("Введите положительноре число!");
}
else
{
 Console.Write(Multiply(numA, numB));   
}
*/

